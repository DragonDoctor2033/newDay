---
worth: yes
where: scripts/test_digest_tools.py:189
added: 2026-09-05
---
# test_digest_tools: проверка «alert line rendered» падает

`check("За сутки было 1 срочных алертов:</b> fresh alert." in html, ...)` не
находит строку в HTML из `publish_digest(dry_run=True)`. Падает и на чистом
HEAD (bf941d2), так что это дрейф между тестом и рендером — скорее всего после
переработки дайджеста 04–05.09.2026 (28d2833 и далее). Остальные 60+ проверок
файла зелёные.

Разобраться, кто прав: если рендер строки алертов поменялся намеренно —
обновить ожидание в тесте; если строка пропала из шапки — вернуть в рендер.
Найдено 05.09.2026 при прогоне тестов после закрытия пунктов бэклога про архив
и search_news.
